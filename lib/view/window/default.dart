import 'package:flutter/material.dart';

class DefaultView extends StatelessWidget {
  const DefaultView({super.key});

  @override
  Widget build(BuildContext context) {
    return (const Center(
      child: Text("Being Developed Soon!"),
    ));
  }
}
